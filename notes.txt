-Git is local repository
-Github is a hosted repository

---Important commands---
=======================================
Scenario 1
=======================================
You start by creating a repository on Github
git clone https://github.com/adi4733/Airflow-Weather-Data-Pipeline.git

I added a new file in fin-project as transformations.py
and made changes to the readme.md


git add . 
To stage all the file so that it can be committed

git commit -m "message"
this commits the file from the staging to the commit stage and the code is ready to be pushed to the github repository

git push origin main

origin - referring to the github repository https://github.com/adi4733/Airflow-Weather-Data-Pipeline.git
main - It is basically the branch on which it will be pushed


HOw to push a new project to Github

step1 - stage all the file
step2 - Commit the file in local
step3 - set origin (link origin to the remote repository) by using the below command
        git remote add origin https://github.com/adi4733/learning-github.git
        this will link your local rep to github repo
step4 - 


how to check to which repo your origin points at
    git remote -v

how to check on which branch we are
    git branch

how to rename master to main (becoz github has main but git has master)
 git branch -M main

How to push code from local to github
 git push origin main 
 (git push -u origin main - one time command to which will help you to push the gode to github with just git push)
 it basically means you are setting up upstream so you just need git push to push your code
 